import type { FileDetails } from '@/types/lib';
const fileDetails: FileDetails = {
  name: 'TreeItem',
  prop: [{ type: 'number', name: 'param', defaultValue: '0', description: 'number of params' }],
};
export default fileDetails;
